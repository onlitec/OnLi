# Alias for OnLi directory
alias.url += (
	"/OnLi" => "/usr/share/OnLi/htdocs",
)

# Disallow access to libraries
#$HTTP["url"] =~ "^/OnLi/libraries" {
#    url.access-deny = ( "" )
#}

# Limit access to setup script
#$HTTP["url"] =~ "^/OnLi/setup" {
#	auth.backend = "htpasswd"
#	auth.backend.htpasswd.userfile = "/etc/OnLi/htpasswd.setup"
#	auth.require = (
#		"/" => (
#			"method" => "basic",
#			"realm" => "OnLi Setup",
#			"require" => "valid-user"
#		)
#	)
#}
